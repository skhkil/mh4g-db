## 2026-09-15 · 채팅 4 방어구 제작 소재 / 아이템 역참조 연결
- 방어구 원본 데이터의 `materials` 필드는 존재했지만 방어구 상세 표에서 표시하지 않던 문제를 수정했습니다.
- 검사/거너/기타 방어구 상세에 **`생산 소재`** 열을 추가하여 각 방어구 제작에 필요한 아이템과 수량을 표시합니다.
- 생산 소재 중 현재 아이템 DB와 이름이 정확히 매칭되는 항목은 클릭 가능한 링크로 표시하며, 선택하면 기존 아이템 상세 역참조로 이동합니다. 아이템 DB에 없는 소재명도 누락하지 않고 일반 텍스트로 그대로 표시합니다.
- 방어구 세트 화면에는 5부위 제작 소재를 아이템별로 합산한 **`세트 제작 소재`**를 추가했습니다. 세트 소재 역시 아이템 DB 매칭 항목은 역참조 링크로 연결됩니다.
- 방어구/세트 검색에서도 제작 소재명을 검색할 수 있도록 유지·확장했습니다.
- 모바일/폴드에서는 `생산 소재`와 `세트 제작 소재`를 전체 폭 영역으로 표시하여 소재명이 세로로 찢어지지 않도록 기존 반응형 규칙에 연결했습니다.
- 방어구 세트 소재 합계는 세트별 메모리 캐시를 사용하며, 데이터 재가져오기 시 캐시를 초기화합니다.
- 캐시 식별자를 `0.7.7-chat4-armor-materials1`로 갱신했습니다.

## 2026-09-15 · 채팅 4 아이템 역참조 성능 구조 재설계
- 이전 최적화에서 실제 병목을 제거하지 못했던 문제를 재점검했습니다. 핵심 병목은 **1.8MB `item_references.json` 전체 파싱/검색 인덱싱**과 **524행 HTML 테이블 안에 상세 행을 삽입하면서 발생하는 전체 table reflow**였습니다.
- 런타임 역참조를 `data/item_refs/<item-id>.json`으로 분할했습니다. 아이템 목록 진입 시에는 약 15KB의 `item_reference_index.json`만 읽고, **선택한 아이템의 작은 역참조 파일만 클릭 시 로드**합니다. 기존 `item_references.json`은 감사/재생성용 원본으로 유지하지만 화면에서는 더 이상 로드하지 않습니다.
- 아이템 목록을 `<table>` 기반에서 **독립 CSS Grid 행 목록**으로 변경했습니다. 상세를 선택 행 아래에 삽입해도 브라우저가 524행 전체의 표 열 너비와 레이아웃을 다시 계산하지 않습니다.
- 아이템 행에 `content-visibility:auto`를 적용하여 화면 밖 행의 렌더/페인트 비용을 줄였습니다.
- 아이템 검색은 역참조 1.8MB 내용을 매번 합치는 방식이 아니라 아이템명/일·영문명/기본 입수/비고의 경량 검색 인덱스만 사용합니다.
- 상세 그룹은 기존처럼 펼칠 때만 DOM을 만들며, 아이템별 역참조 파일과 상세 HTML은 메모리 캐시해 같은 아이템 재클릭 시 재요청/재조립하지 않습니다.
- 투명 링크 버튼에 전역 `button:hover`의 `filter:brightness()`가 적용되지 않도록 제외하여 포인터 이동 시 불필요한 페인트도 줄였습니다.
- `tools/build_item_references_v0.7.7.py`도 단일 감사 JSON과 런타임용 분할 JSON/15KB 인덱스를 함께 생성하도록 수정했습니다.
- 브라우저 History API 동작과 선택 아이템 바로 아래 상세 표시 방식은 유지합니다.
- 캐시 식별자를 `0.7.7-chat4-itemxref4-perf`로 갱신했습니다.
- 검증: `app.js`/`data-loader.js` 문법 검사, 런타임 JSON 371개 파싱, 역참조 347개 분할 파일 생성, 최대 단일 역참조 파일 약 44KB를 확인했습니다.

## 2026-09-15 · 채팅 4 아이템 역참조 2차 최적화 / 인라인 상세 / 뒤로가기
- 아이템 클릭 시 상세 패널이 표 상단에 생기면서 524행 전체 위치를 다시 계산하던 구조를 제거하고, **선택한 아이템 행 바로 아래에 상세가 펼쳐지는 인라인 구조**로 변경했습니다.
- 역참조 상세의 `퀘스트 보수 / 몬스터 갈무리·보수 / 조합 / 용인족 도매상 / 무기 / 방어구 / 장식주` 목록을 처음부터 전부 DOM으로 만들지 않고, **그룹 제목과 건수만 먼저 표시한 뒤 사용자가 펼칠 때 해당 목록만 생성하는 지연 렌더링**으로 변경했습니다.
- 상세를 열 때 대량 역참조 DOM 생성과 표 전체 재배치 비용을 줄여 아이템 클릭 시 포인터/스크롤이 순간적으로 끊기는 현상을 완화했습니다.
- 초기 화면 뒤에서 약 1.8MB의 역참조 JSON을 임의로 파싱하던 prewarm을 제거했습니다. 역참조 데이터는 아이템 화면 진입 또는 아이템 링크 첫 사용 시에만 로드하여 다른 화면 사용 중 예기치 않은 메인 스레드 정지를 줄였습니다.
- 브라우저 History API를 추가했습니다. 아이템 상세 진입 전 상태를 현재 히스토리에 보존하고 상세/역참조 이동을 새 히스토리 상태로 기록하여, **마우스 뒤로가기/브라우저 뒤로가기 시 사이트 밖으로 빠지지 않고 직전 MH4G DB 화면·아이템 상태로 복귀**합니다.
- 아이템 상세의 사용처에서 무기/방어구/퀘스트 등으로 이동한 뒤에도 뒤로가기를 누르면 이전 아이템 상세로 돌아오도록 연결했습니다.
- 모바일/폴드 반응형 표에서도 인라인 상세 행이 일반 2~3열 카드 규칙에 찢어지지 않고 전체 폭으로 표시되도록 전용 CSS를 추가했습니다.
- 캐시 식별자를 `0.7.7-chat4-itemxref3-inlinehist`로 갱신했습니다.
- 검증: `app.js`/`data-loader.js` 문법 검사, 전체 JSON 파싱, CSS 괄호 정합성, 잔여 상단 고정 `itemDetail` 호스트 제거 여부를 확인했습니다.

## 2026-09-15 · 채팅 4 아이템 역참조 클릭 성능 최적화
- 아이템 역참조 1차 구현에서 아이템 링크 클릭 시 순간적으로 마우스/화면이 버벅일 수 있던 렌더링 경로를 점검하고 최적화했습니다.
- 다른 DB 화면에서 아이템을 눌렀을 때 `아이템 524행 전체 렌더 → 검색값 적용 → 재렌더`가 연속으로 발생하던 **이중 렌더링을 제거**했습니다. 이제 검색값을 먼저 지정하고 필요한 결과만 한 번 그립니다.
- `item_references.json`의 입수처/사용처 검색 문자열을 클릭·검색 때마다 재조합하지 않고 **아이템별 검색 코퍼스를 메모리에 1회 인덱싱**하여 재사용합니다.
- 아이템 상세 HTML은 아이템별로 최초 1회 생성 후 캐시하여 같은 아이템을 다시 열 때 역참조 그룹 필터링과 HTML 조립을 반복하지 않습니다.
- 무기/장식주/몬스터/조합/용인족 도매상 등에 생성되는 다수의 아이템 링크에 각각 `onclick`을 붙이던 방식을 제거하고 **document 단일 이벤트 위임** 방식으로 변경했습니다. 아이템 상세의 사용처 링크도 같은 방식으로 처리합니다.
- 상세 패널 이동의 `smooth` 스크롤을 제거하고, 화면 밖에 있을 때만 즉시 이동하도록 변경하여 모바일/폴드에서 스크롤 애니메이션 중 메인 스레드 프레임 저하를 줄였습니다.
- `itemById` 인덱스를 추가하여 상세 아이템 조회 시 배열 전체 검색을 하지 않습니다.
- 초기 화면 렌더가 끝난 뒤 브라우저 유휴 시간에 아이템/역참조 JSON을 미리 로드하여 **첫 아이템 클릭의 1.8MB 역참조 JSON 로딩·파싱 지연도 앞당겨 처리**합니다.
- 캐시 식별자를 `0.7.7-chat4-itemxref2-opt`로 갱신했습니다.
- 검증: `app.js`/`data-loader.js` 문법 검사, 전체 JSON 파싱, 잔여 개별 아이템 클릭 바인딩 여부를 확인했습니다.

## 2026-09-15 · 채팅 4 아이템 중심 역참조 1차 구축
- 아이템 DB에서 아이템명을 선택하면 **입수처와 사용처를 한 화면에서 확인하는 상세 역참조 패널**을 표시하도록 추가했습니다.
- 입수처 연결: 이벤트/에피소드 퀘스트의 구조화된 주요 보수, 몬스터 갈무리·보수, 조합 결과, 용인족 도매상 판매/증식/교환 데이터를 연결합니다.
- 사용처 연결: 무기 생산·강화, 방어구 생산, 장식주 생산, 조합 재료, 용인 교환 재료를 연결합니다.
- 역참조 항목의 무기/방어구/장식주/퀘스트/몬스터/조합/용인족 도매상 이름을 누르면 해당 DB 화면으로 이동하여 대상 항목이 검색되도록 연결했습니다.
- 반대 방향 탐색도 추가했습니다. 무기 생산/강화 소재, 장식주 소재, 몬스터 보수/소재, 조합 결과·재료, 용인족 도매상 아이템 중 현재 아이템 DB와 정확히 매칭되는 이름은 클릭하여 아이템 상세로 이동할 수 있습니다.
- `data/item_references.json` 역참조 인덱스를 새로 생성했습니다. 아이템 524개 중 구조화 데이터로 연결이 확인되는 347개를 인덱싱했으며, 입수처 989건 / 사용처 5,315건을 연결했습니다.
- 재현 스크립트 `tools/build_item_references_v0.7.7.py`와 감사 결과 `tools/item_reference_audit_v0.7.7.json`을 추가했습니다.
- 표본 검증: `길드티켓S` 입수 3 / 사용 53, `뱀·개구리티켓` 입수 1 / 사용 10으로 현재 프로젝트 데이터와 일치함을 확인했습니다.
- 데이터/CSS 캐시 식별자를 `0.7.7-chat4-itemxref1`로 갱신했습니다.
- PC 레이아웃은 유지하고, 아이템 상세 역참조 패널은 모바일/폴드에서 1열 구조로 자동 전환되도록 반응형 처리했습니다.

## 2026-09-15 · 채팅 4 모바일/폴드 좌측 메뉴 드롭다운 정렬 보정
- 모바일/폴드 좌측 메뉴가 CSS Grid의 같은 행 높이를 공유해, 한 메뉴의 드롭다운을 펼치면 같은 행의 다른 메뉴까지 같이 늘어나고 아래 정렬이 밀리던 문제를 수정했습니다.
- 모바일/폴드에서 좌측 메뉴를 **서로 독립적인 세로 컬럼**으로 재배치합니다. 600px 미만은 2열, 600px 이상 폴드 내부/소형 태블릿은 3열을 사용합니다.
- 한 컬럼에서 `무기`, `검사방어구`, `거너방어구`, `장신구`, `몬스터`, `용인족 도매상`, `퀘스트` 등의 하위 메뉴를 펼쳐도 **다른 컬럼의 메뉴 높이와 위치는 변하지 않습니다.**
- PC 메뉴 구조와 동작은 기존 상태를 유지하며, 모바일/폴드 조건에서만 독립 컬럼 레이아웃을 적용합니다.
- CSS/데이터 캐시 식별자를 `0.7.7-chat4-navfix1`로 갱신했습니다.

## 2026-09-15 · 채팅 4 모바일/폴드 반응형 전면 재정비
- 이전 `max-width: 820px` 단일 기준 때문에 폴드 내부 화면에서 PC 표가 노출되던 문제를 수정했습니다. 터치 기기는 최대 1180px까지 폴드/모바일 레이아웃을 적용하고, 일반 PC는 기존 데스크톱 레이아웃을 유지합니다.
- 좌측 메뉴의 DB 화면 전체를 공통 점검했습니다: 무기/속성별 무기요약/수렵피리 선율, 검사·거너 방어구/세트, 장신구, 스킬, 식사, 몬스터 상세·보수·소재, 용인족 도매상, 아이템, 조합서, 퀘스트.
- 모바일 표를 기존의 긴 1열 세로 카드에서 **컴팩트 2열(폴드 외부/폰)·3열(폴드 내부/소형 태블릿) 카드**로 재구성했습니다. 이름·스킬·내성·예리도·소재·비고 등 긴 정보만 카드 전체 폭을 사용합니다.
- 무기 DB는 공격력/속성·특수/회심/슬롯/등급 등 핵심 수치가 접힌 화면에서도 함께 보이도록 했고, 예리도는 전체 폭 게이지, 생산/강화 소재는 가로 줄바꿈 블록으로 표시합니다.
- 방어구 및 나머지 DB도 같은 정보 우선순위 규칙을 적용해 한 글자씩 세로로 깨지는 현상과 불필요한 가로 스크롤을 줄였습니다.
- 420px 이하 폴드 외부 화면에서는 여백·글자·소재 라벨을 한 단계 더 압축합니다.
- 데이터/CSS 캐시 갱신을 위해 캐시 키를 `0.7.7-chat4-mobile2`로 변경했습니다.
- 검증: `app.js`/`data-loader.js` JavaScript 문법 검사, CSS 파서 오류 0건 및 괄호 정합성 검사를 완료했습니다.

## 2026-09-15 · 채팅 4 비고 정리 보정
- 이벤트 하위/상위/G급, 에피소드, 다운로드 챌린지의 비고에서 제작·대조·검증용 내부 메모를 제거했습니다.
- 에피소드 비고의 `에피소드: ...` 표기는 유지합니다.
- 확인 가능한 이벤트 퀘스트는 주요 보수 아이템과 해당 소재의 장비 사용처를 비고에 유지합니다.
- 다운로드 챌린지는 제공 MH4U DB의 해당 DLC 구간에 아이템 보수 행이 없어, 확인되지 않은 아이템 보수를 임의로 작성하지 않았습니다.
- 이벤트 생성 도구(`tools/add_event_quests_v0.7.7.py`)에서도 내부 메모 생성 로직을 제거하여 재생성 시 되살아나지 않도록 수정했습니다.
- 데이터 로더 캐시 버전을 `0.7.7-chat4-fix2`로 변경하여 기존 0.7.7 브라우저 캐시가 새 `quests.json`을 가리지 않도록 했습니다.
# MH4G DB + 스킬 시뮬레이터 v0.7.7


## v0.7.7 4차 작업 — 퀘스트 비고/요약 UI 정리

- 기준 파일 정책: **1번 프로젝트 원본을 수정 대상**으로 사용하고, **2번 Athena Data DB / 3번 MH4U DB는 비교·검증용 참조 데이터**로만 사용했습니다.
- 퀘스트 `이벤트 / 에피소드 / 다운로드 챌린지`의 비고에서 제작·검증 과정에만 필요한 내부 문구를 제거했습니다.
- 이벤트/에피소드 퀘스트는 MH4U DB에서 확인 가능한 경우 **주요 보수 아이템**과 **해당 아이템을 재료로 사용하는 장비/아이템 사용처**를 비고에 추가했습니다.
- 에피소드 비고는 기존 식별 문구인 **`에피소드: xxxxx`를 유지**했습니다.
- 다운로드 챌린지 24개는 제공 MH4U DB의 `arena_rewards`에서 대응 보수 행을 확인할 수 없어, 보수 아이템을 추측해 추가하지 않고 기존 내부 검증용 비고만 제거했습니다. 퀘스트 표의 목표·보수금·인원/지정장비 조건은 유지했습니다.
- 퀘스트 메뉴의 **`여단 요약` / `집회소 요약` / `G급 요약`** 항목과 관련 진입 로직을 제거했습니다.
- 몬스터 메뉴의 **`육질표 요약`** 항목과 관련 진입 로직을 제거하고, 몬스터 기본 진입 화면을 **육질표 상세**로 변경했습니다.
- 검증: 프로젝트 JSON 파일 파싱 및 `app.js` 문법 검사를 수행했습니다.
- 이 항목부터는 **기능·데이터 수정이 발생한 모든 작업에서 README와 WORK_STATUS를 함께 갱신**하는 것을 릴리스 절차로 적용합니다.





## v0.7.7 이벤트 퀘스트 한글화/확장

- 해외판 MH4U DLC 기준 이벤트 퀘스트를 퀘스트 DB에 추가했습니다.
  - 하위 이벤트 6개
  - 상위 이벤트 27개
  - G급 이벤트 45개
  - 에피소드 이벤트 18개
  - 다운로드 챌린지 24개
  - 기존 퀘스트 427개 + DLC 120개 = 총 547개
- 사용자가 제공한 상위 이벤트 화면의 영문 퀘스트 28개(에피소드 `Bonus: Stash Grab` 포함)를 전부 대조해 수록했습니다.
- 퀘스트 메뉴에 `이벤트 전체 / 이벤트 하위 / 이벤트 상위 / 이벤트 G급 / 에피소드 / 다운로드 챌린지` 보기를 추가했습니다.
- 이벤트 상세표에 목표, 장소, 계약금, 보수금, HRP, 제한시간, 서브퀘스트/보수/HRP, 특수조건을 표시하고 한글/일본어/영어로 검색할 수 있게 했습니다.
- 사용자 제공 `mh4u.db`와 수치·영문명을 교차검증했습니다. Athena `Data.zip`에는 이벤트 퀘스트 본문 데이터가 없었지만 이벤트 전용 아이템(`カジキ`, `オオクワガタ`) 존재는 확인했습니다.
- 로컬라이즈된 공개 자료와 `mh4u.db`가 충돌하는 일부 DLC 수치는 해외판 로컬라이즈 자료를 우선했습니다. 충돌 내역은 `tools/event_quests_v0.7.7.json`에 기록했습니다.
- 재현 스크립트: `tools/add_event_quests_v0.7.7.py`
- 감사 기록: `tools/event_quests_v0.7.7.json`

## v0.7.6 한글 메인 + 일본어/영어 서브 1차 확장

- 표시 정책을 핵심 DB 전반에 **한국어 주표기 + `日本語 · English` 보조표기**로 확장했습니다.
- 기존 한국어 `name` 필드는 바꾸지 않았습니다. 검증 결과 **한국어 문구 변경 0건**입니다.
- 기존 v0.7.5 방어구 다국어 데이터는 그대로 유지했습니다: 영어명 **2,850 / 2,876**.
- 무기 `name`에 한국어와 일본어가 한 필드로 붙어 있던 **혼합 표기 7개**는 한국어 부분만 메인 `name`에 남기고, 일본어를 `nameJa`로 분리했습니다. 한국어 문구 자체의 변경은 0건입니다.
- 이번 확장 범위:
  - 무기 영어명 **2,268 / 2,314**
    - MH4U 일본명 정확 매칭 2,245
    - v0.7.5에서 이미 스탯까지 교차검증한 verified alias 23
    - 남은 46개는 원본 일본명 오타/누락이 있어 **새 fuzzy 추측 매칭을 하지 않고 보류**
  - 장식주 영어명 **199 / 199**
  - 스킬 계통 영어명 **143 / 143**
  - 스킬 발동명 영어명 **276 / 276**
  - 아이템 영어명 **515 / 524**, 남은 9개는 안전한 일본명 exact-match가 없어 보류
- Athena `Data.zip`와 MH4U DB를 독립 대조했습니다. 현재 프로젝트 장식주 199개는 두 소스의 영어명이 전부 일치했고, 스킬 계통은 Athena에서 일본명 exact-row로 연결되는 139개가 전부 일치했습니다.
- 무기/방어구/장식주/스킬/아이템 목록 및 시뮬레이터 검색 선택창은 한국어를 메인으로 유지하면서 일본어/영어 검색도 지원합니다.
- 상세 감사 기록: `tools/core_localization_v0.7.6.json`
- 재현 스크립트: `tools/enrich_core_localization.py --project <project> --mh4u <mh4u.db> --athena <Data.zip>`

## v0.7.5 작업 내용

- **수렵피리 선율표 위치 변경**
  - 무기 메뉴의 독립 항목이던 `선율표`를 `수렵피리` 바로 아래의 종속 메뉴로 이동했습니다.
  - 선율표 기능과 검색은 기존대로 유지합니다.
- **무기 DB 정렬 기능 추가**
  - 공격력 높은/낮은 순, 기본 예리도, 예리도+1, 속성값, 회심, 슬롯, 이름 기준 정렬을 추가했습니다.
  - 파생 순서 보기 역시 기존 기본 정렬로 유지합니다.
- **무기 속성 필터 추가**
  - 불 / 물 / 번개 / 얼음 / 용 / 독 / 마비 / 수면 / 폭파 / 무속성으로 필터링할 수 있습니다.
  - 검색어, 무기종, 파생 필터와 함께 조합해서 사용할 수 있습니다.
- **누락 예리도 외부 데이터 보강 완료**
  - v0.7.4 감사 기준 완전 누락 568개 + 부분 누락 27개, 총 **595개**의 근접무기 예리도를 공개 `MonsterHunter4UDatabase`의 `mh4u.db`와 대조해 보강했습니다.
  - **569개는 일본명 + 무기종 정확 매칭**, 나머지 **26개는 기존 원본의 일본명 오타/깨짐을 공격력·속성(또는 각성)·슬롯·회심 및 유사 일본명으로 교차검증한 명시적 alias 매칭**입니다.
  - 외부 DB의 예리도는 빨강/주황/노랑/초록/파랑/흰색/보라 실제 구간 값을 그대로 저장하며, 추측/보간 값은 사용하지 않았습니다.
  - 기존에 양쪽 게이지가 완전했던 **1,264개는 수정하지 않았고**, 비교 검증 결과 예리도 외 다른 무기 필드 변경도 없습니다.
  - 최종 감사 결과: 근접무기 **1,859 / 1,859 완전**, 부분 0, 누락 0, 구조 오류 0.
  - 병합 내역은 `tools/mh4u_sharpness_merge.json`, 최종 감사 결과는 `tools/sharpness_audit.json`에서 확인할 수 있습니다.
  - 외부 데이터 출처: `https://github.com/kamegami13/MonsterHunter4UDatabase` (`app/src/main/assets/databases/mh4u.db.zip`)

### v0.7.5 방어구 3자 교차검증 및 보정 완료

- 방어구는 **현재 MH4G DB ↔ 사용자 제공 Athena 데이터 ↔ MonsterHunter4UDatabase** 3자 대조로 검증했습니다.
- **`maxDefense`와 `upgradeDefense`는 강화 기준이 다르므로 비교/수정 대상에서 완전히 제외**했습니다. 최종 검증 결과 두 필드는 **변경 0건**입니다.
- 한국어 장비명, 소재명, 랭크, 가격은 그대로 유지했습니다.
- 일본명 누락 **995개를 복원**하고 기존 일본명 오타/깨짐 **37개를 정정**했습니다. 일본명 미기재는 1,028개 → **33개**로 감소했습니다.
  - Athena 한국어 행과 MH4U 일본명/부위를 독립적으로 확인한 매칭을 우선 사용했습니다.
  - 남은 항목 중 38개는 부위·희귀도·기본방어력·슬롯·성별·검사/거너·5속성내성·몸통배가·스킬의 전체 서명이 두 외부 소스에서 유일하게 일치할 때만 보완했습니다.
- 두 외부 소스가 **같은 값으로 일치한 경우에만** 실제 수치를 보정했습니다. 총 209개 방어구에서 296개 필드가 수정되었습니다.
  - 기본 방어력 5
  - 슬롯 7
  - 성별 77
  - 화내성 17 / 수내성 12 / 뇌내성 56 / 빙내성 76 / 용내성 15
  - 스킬 구성/포인트 31
- 스킬 일본명 메타데이터도 외부 두 소스가 일치하는 `체술: 体術`, `유탄추가: 榴弾追加` 2건을 정정했습니다.
- 수정 후 2,876개 중 **2,842개를 Athena + MH4U 양쪽에 고신뢰 연결**했으며, 34개는 자동 수정하지 않고 보류했습니다.
- 외부 두 소스가 서로 다른 항목(검사/거너, 일부 기본방어력/스킬/내성/슬롯)은 **자동 덮어쓰기하지 않았습니다**.
- 상세 병합 기록은 `tools/armor_crosscheck_merge_v0.7.5.json`에 저장했습니다. 재현용 스크립트는 `tools/merge_armor_crosscheck.py`입니다.
- **방어구 표시 언어 정책을 `한글 메인 + 일본어/영어 서브`로 고정**했습니다.
  - 기존 한국어 `name`은 변경하지 않았으며 일본어 문자가 메인 이름에 들어간 방어구는 0개입니다.
  - Athena 언어 행을 이용해 `nameEn`을 2,850개 방어구에 추가했습니다. 일본명 정확 행 2,842개 + 한국어 정확 행 8개입니다.
  - 26개는 영어명을 안전하게 연결할 근거가 부족해 추측하지 않고 미기재로 남겼습니다.
  - 방어구 DB 목록과 수동 장비 선택창은 한국어 이름을 주표기로 사용하고, 보조줄에 `日本語 · English`를 표시/검색합니다.
  - 상세 기록은 `tools/armor_localization_v0.7.5.json`, 재현 스크립트는 `tools/enrich_armor_localization.py`입니다.

## v0.7.4 예리도 감사 / 릴리스 정합성

- 근접무기 11종의 예리도 데이터를 전수 감사하고 `tools/sharpness_audit.json`에 결과를 저장했습니다.
- v0.7.4 당시 근접무기 1,859개 중 예리도 객체가 있는 무기는 1,291개였으며, 양쪽 게이지가 모두 복원된 무기는 1,264개였습니다.
- v0.7.4에서는 원본 게이지가 프로젝트 ZIP에 포함되지 않은 568개를 추측해서 채우지 않았고, 부분 복원 27개도 별도 목록으로 남겼습니다. 이 595개는 v0.7.5에서 외부 MH4U DB로 보강했습니다.
- `sim_armors.json`, `sim_weapons.json`, `sim_armor_sets.json`을 원본 데이터에서 다시 동기화하고 필드 단위 일치 여부를 검증했습니다.
- 데이터 캐시 버전과 빌드 메타 버전을 v0.7.4로 통일했습니다.

## v0.7.3 UI 정리

- 왼쪽 메뉴의 `메인`, `이력`, `데이터 관리` 항목을 제거했습니다.
- 사이드바 하단의 데이터 버전 / 원본 Source 표시를 제거했습니다.
- 상단 헤더의 버전 배지와 최적화 안내 문구를 제거했습니다.
- 기능과 데이터는 그대로 유지하며 화면의 불필요한 운영/개발 정보를 숨겼습니다.

## v0.7.2 성능 최적화

- GitHub Pages 첫 화면은 `sim_armors.json` / `sim_weapons.json` / `sim_armor_sets.json` 경량 데이터를 사용합니다.
- 초기 데이터 요청을 순차 `await` 방식에서 `Promise.all` 병렬 로딩으로 변경했습니다.
- `cache: no-store`를 제거하고 버전 쿼리 + 브라우저 캐시를 사용합니다.
- 몬스터/퀘스트/아이템/식사/용인족 도매상 등은 해당 메뉴를 열 때만 원본 JSON을 지연 로딩합니다.
- 무기/방어구/장식주/스킬 ID 조회는 `Map` 인덱스로 바꾸고, 수동 장비 검색 옵션은 캐시합니다.
- 장비/장식주 하나를 변경할 때 수동 장비 UI 전체를 다시 만드는 동작을 줄이고 해당 행/결과만 갱신합니다.
- DB 화면의 타입/등급 필터 변경이 시뮬레이터 UI를 불필요하게 다시 그리지 않도록 분리했습니다.
- `tools/build_database.py --publish` 실행 시 경량 시뮬레이터 JSON도 함께 재생성됩니다.

## v0.7.1 수정사항

- 시뮬레이터의 무기/머리/몸통/팔/허리/다리/호석 각 행에 해당 파츠의 스킬 포인트 기여량 표시
- 장비 자체 스킬 포인트와 장식주 포인트를 분리 표시해서 상단 기여량을 합치면 아래 총합과 맞도록 구성
- 몸통배가 사용 시 몸통의 실제 기여 포인트를 배수 적용해서 표시
- 장비 결과 상단 요약에 `파츠별 스킬 포인트` 열 추가
- 장비 결과의 `장식주` 행에는 전체 장식주 스킬 포인트 합계를 별도로 표시(몸통 슬롯 장식주는 몸통배가 반영)

## v0.6.8 변경사항
- 스킬 시뮬레이터의 수동 장비 계산 `무기` 행에 `무기 종류` 필터를 추가했습니다.
- `전체 무기 / 대검 / 태도 / 한손검 / 쌍검 / 해머 / 수렵피리 / 랜스 / 건랜스 / 슬래시액스 / 차지액스 / 조충곤 / 라이트보우건 / 헤비보우건 / 활` 중 하나를 먼저 선택하면 해당 종류의 무기만 검색 후보에 표시됩니다.
- 무기 종류를 바꾸면 현재 선택 무기가 다른 종류일 경우 무기와 무기 장식주 선택을 자동 초기화합니다.
- 선택한 무기 종류 필터도 장비 세팅 저장/불러오기에 포함됩니다.

## v0.6.7 변경사항
- 무기 DB 검색창 앞에 `무기 종류` 드롭다운 배치. 무기 종류 필터는 헤더 전역필터와 독립적으로 동작합니다.
- 상단 헤더의 `타입 / 등급` 드롭다운을 화면에서 제거했습니다.
- 방어구 등급을 **최초 제작 가능 진행도** 기준으로 전수 재판정했습니다.
  - 기존 문제: 원본 `m1/r1`은 RARE 1~4 묶음인데 RARE4의 상위 장비까지 일괄 하위 처리됨.
  - 수정: S 계열, 원본 아이템/갈무리/용인족 교환표의 소재 입수 등급, 확인된 RARE4 하위 예외를 함께 사용.
  - 2,876개 중 234개가 `하위 → 상위`로 보정됨.
  - 기본 디아블로 세트(RARE3)는 하위가 맞아 그대로 유지.
  - 마기르 RARE4는 머리/다리=상위, 몸통/팔/허리=하위로 부위별 판정.
- 판정 근거와 변경 목록은 `tools/armor_rank_audit.json`에 저장했습니다.

## v0.6.7 장비 결과 레이아웃 개선
- 장비 결과 패널 내부의 가로/세로 스크롤 제거
- 결과 표를 패널 폭 100% 안에서 고정 레이아웃으로 표시
- 효과 설명은 좁은 열에서 자동 줄바꿈
- 포인트 열 제목/값을 같은 고정 열에 중앙 정렬

## v0.6.7 UI 가독성 개선
- 검색형 드롭다운은 한 번에 하나만 열립니다. 다른 검색 박스를 누르면 기존 목록이 자동으로 닫힙니다.
- 호석 스킬 목록은 설명을 제거하고 스킬명만 가로 한 줄로 표시합니다.
- 장식주 검색 목록은 넓은 팝업으로 표시하며 명칭/슬롯/포인트가 세로로 깨지지 않게 했습니다.
- 장비 결과의 `스킬 계통`과 `발동 스킬`은 줄바꿈을 억제해 가로로 표시합니다.

## v0.6.7 스킬 결과/검색 개선
- 장비 결과 스킬 포인트 상태: 발동 초록 / 미달 주황 / 음수 빨강
- 결과표 4열: 스킬 계통 / 포인트 / 발동 스킬 / 실제 효과
- 미발동 스킬은 다음 발동 포인트와 다음 효과 표시
- 장식주 검색, 호석 스킬 검색, 자동조합 목표 스킬 검색에서 발동명/효과 문구까지 검색
- `회심` 검색은 회심 관련 스킬과 사용자 편의를 위한 통격(약점특효) 관련 후보도 노출. 단 MH4G 약점특효 실제 효과는 육질 45 이상 부위에 육질 +5로 표시
- 스킬 결과 제목/행 정렬 고정


## v0.6.7 레이아웃 수정
- `원하는 스킬`과 `자동 조합 검색`을 다시 하나의 패널로 결합했습니다.
- 이 통합 패널 전체를 수동 장비 계산/장비 결과 아래쪽에 배치했습니다.
- 스킬 선택 → 선택 스킬 칩 → 검색 옵션 → 조합 검색 → 결과가 같은 영역에서 이어집니다.


## 이번 버전 변경
- 자동 조합 검색을 시뮬레이터 맨 아래 위치로 복원
- 수동 장비 계산을 2줄형 컴팩트 카드로 변경
  - 1줄: 무기/방어구/호석 선택
  - 2줄: 슬롯 현황 + 장착 장식주 + 장식주 검색 추가
- 수동 장비 계산 : 장비 결과 비율을 6:4로 변경
- 기존 실제 MH4G DB와 세팅 저장/불러오기 기능 유지
- `run_site.bat`은 빈 포트를 자동 선택하며 캐시를 비활성화

## 실행
`run_site.bat`을 실행하세요. v0.7.3부터 화면의 버전 배지는 표시하지 않습니다.

## 2026-09-15 모바일 / 폴드 반응형 개선
- PC 화면(821px 이상)의 기존 레이아웃과 표 형태는 유지합니다.
- 820px 이하 모바일/폴드 화면에서는 데이터 표를 가로 스크롤 표가 아닌 세로 카드형으로 자동 변환합니다.
  - 각 셀은 `항목명 : 값` 형태로 표시됩니다.
  - 값이 비어 있거나 `-`인 항목은 모바일에서 숨겨 불필요한 세로 길이를 줄입니다.
- 적용 대상: 퀘스트(이벤트/에피소드/챌린지 포함), 무기, 방어구, 장식주, 스킬, 아이템, 몬스터 상세/보수, 교환, 조합 등 주요 DB 표.
- 무기표의 생산/강화 소재, 예리도 등 데스크톱 전용 최소 폭을 모바일에서 해제했습니다.
- 모바일 툴바/검색/상단 조작부를 화면 폭 100% 안에서 재배치하고, 장식주 칩도 줄바꿈하도록 수정했습니다.
- 420px 이하의 좁은 폴드 외부 화면에서는 여백과 글자 크기를 추가로 축소합니다.
- CSS/데이터 캐시 키를 `0.7.7-chat4-mobile1`로 갱신해 이전 캐시가 새 반응형 UI를 가리지 않도록 했습니다.
